spring:
  application:
    name: BlogAPI
  datasource:
#    url: jdbc:mysql://localhost:3306/blog_db
#    username: root
#    password: root
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false